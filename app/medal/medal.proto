syntax = "proto3";

package medal;
option go_package="./medal";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Medal {
  rpc Ping(Request) returns(Response);
}
